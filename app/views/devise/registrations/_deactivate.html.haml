#deactivate-modal.modal.fade{:role => 'dialog', :tabindex => '-1'}
  .modal-dialog{:role => 'document'}
    .modal-content
      .modal-header
        %button.close{'aria-label' => 'Close', 'data-dismiss' => 'modal', :type => 'button'}
          %span{'aria-hidden' => 'true'} ×
        %h4.modal-title Anular mi cuenta
      .modal-body
        %p.text-center
          Para eliminar su cuenta, presione el siguiente boton.
          %strong Esta accion es irreversible

        %p.text-center
          = link_to 'Anular mi cuenta', registration_path(resource_name), method: :delete, class: 'btn btn-danger'
      .modal-footer
        %button.btn.btn-default{'data-dismiss' => 'modal', :type => 'button'} Cancelar